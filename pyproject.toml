[project]
name = "vehicle-parts"
version = "0.1.0"
description = "Vehicle Parts Analysis"
requires-python = ">=3.8"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = []
